const person = {
	name:"Muneer",
	age:22
}
// literals arebackticks {advanatge : can make multi line strings, add single or double quotes.can add js using $//
const greeting = `Hello, my name ${person.name}! 
I am ${person.age} years old.`;

console.log(greeting);