const person = {
	name:"Muneer",
	age:22
}
// literals are backticks (advanatge : can make multi line strings, can add single or double quotes.can add js using $)//
const greeting = `Hello, my name ${person.name}! 
I am ${person.age} years old.`;

console.log(greeting);
